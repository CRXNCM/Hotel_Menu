# Local dev: relative path uses Vite proxy → backend :5000
VITE_API_URL=/api

# Or full URL — must end with /api (this form is auto-fixed if you omit /api):
# VITE_API_URL=http://127.0.0.1:5000/api
# VITE_API_URL=https://your-service.onrender.com/api
